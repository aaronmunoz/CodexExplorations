{
  "name": "codex-explorations",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "nx": "nx",
    "clean:all": "rimraf packages/*/dist packages/*/node_modules .nx"
  },
  "devDependencies": {
    "lerna": "^8.0.0",
    "nx": "^18.0.0",
    "rimraf": "^6.0.1"
  }
}
